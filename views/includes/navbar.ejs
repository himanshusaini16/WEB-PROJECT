
  <style>
    .btn-search {
      background-color: #fe424d;
      color: white;
      border-radius: 25px;
      padding: 0 1rem;
    }
    .btn-search:hover {
      color: white;
      background-color: #fe424d;
    }
    .btn-search i {
      margin-right: 0.5rem;
      display: inline;
    }
    .search-input {
      border-radius: 25px;
      padding: 0.5rem 1rem;
      font-size: 0.9rem;
    }

    @media (max-width: 767px) {
      .search-form-collapse {
        position: absolute;
        top: 60px;
        left: 0;
        right: 0;
        background: white;
        padding: 1rem;
        border-radius: 0 0 15px 15px;
        box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        z-index: 1000;
        display: flex;
        align-items: center;
      }
      .search-form-collapse .search-input {
        flex: 1;
        margin-right: 1rem;
      }
    }

    .icon{
      border-radius: 50px;
      margin-left: 5px;
    }
  </style>


<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><i class="fa-regular fa-compass"></i></a>
      <form class="d-flex search-input" role="search" action="/listings/search" method="get">
        <input class="form-control search-input d-md-none" type="search" placeholder="Search Destination" name="search">
        <button class="btn btn-search d-md-none icon" type="submit"><i class="fa-solid fa-magnifying-glass"></i></button>
      </form>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <!-- Search icon button -->
      
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-link" href="/listings">Explore</a>
        </div>
        <div class="navbar-nav ms-auto d-none d-md-flex">
          <form class="d-flex search-input" role="search" action="/listings/search" method="get">
            <input class="form-control me-2 search-input" type="search" placeholder="Search Destination" name="search">
            <button class="btn btn-search" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
          </form>
        </div>
        <div class="navbar-nav ms-auto">
          <a class="nav-link" href="/listings/new">Add New Place</a>
          <% if(!currUser){ %>
          <a class="nav-link" href="/signup"><b>SignUp</b></a>
          <a class="nav-link" href="/login"><b>Login</b></a>
          <% } %>
          <% if(currUser){%>
          <a class="nav-link" href="/logout"><b>LogOut</b></a>
          <% } %>
        </div>
      </div>
      <!-- Collapsible search form for mobile view -->
      <div class="collapse search-form-collapse" id="searchForm">
        <form class="d-flex search-input" role="search" action="/listings/search" method="get">
          <input class="form-control search-input" type="search" placeholder="Search Destination" name="search">
          <button class="btn btn-search" type="submit"><i class="fa-solid fa-magnifying-glass"></i>Search</button>
        </form>
      </div>
    </div>
  </nav>
